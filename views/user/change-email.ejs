<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Change Email - iStore</title>
    <link rel="stylesheet" href="/css/components/header.css">
    <link rel="stylesheet" href="/css/components/footer.css">
    <link rel="stylesheet" href="/css/profile.css">
    <link rel="stylesheet" href="/css/change-email.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&display=swap" rel="stylesheet">
    <script src="https://unpkg.com/lucide@latest"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.2/gsap.min.js"></script>
</head>
<body>
    <!-- Header -->
    <%- include("../partials/user/header") %>


    <main class="change-email-main">
        <div class="change-email-container">
            <!-- Breadcrumbs -->
            <nav class="breadcrumb">
                <a href="/">Home</a>
                <div class="breadcrumb-separator">
                    <svg width="5" height="8" viewBox="0 0 5 8" fill="none"><path d="M1 1L4 4L1 7" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/></svg>
                </div>
                <a href="/profile">My Account</a>
                <div class="breadcrumb-separator">
                    <svg width="5" height="8" viewBox="0 0 5 8" fill="none"><path d="M1 1L4 4L1 7" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/></svg>
                </div>
                <span class="breadcrumb-current">Change Email</span>
            </nav>

            <!-- Header Section -->
            <header class="change-email-header">
                <h1>Change Email</h1>
                <p>Update your email address securely.</p>
            </header>

            <!-- Form Card -->
            <div class="change-email-card">
               <form
class="change-email-form"
action="/send-email-change-otp"
method="POST">

<!-- Current Email -->
<div class="form-group">
<label>Current Email</label>

<div class="input-wrapper read-only">

<input
type="email"
value="<%= user.email %>"
readonly>

</div>

</div>

<!-- New Email -->
<div class="form-group">

<label>New Email Address</label>

<div class="input-wrapper">

<input
type="email"
name="newEmail"
placeholder="Enter new email address"
required>

</div>

</div>

<!-- Confirm Email -->
<div class="form-group">

<label>Confirm New Email</label>

<div class="input-wrapper">

<input
type="email"
name="confirmEmail"
placeholder="Re-enter new email address"
required>

</div>

</div>

<div class="action-buttons">

<button
type="submit"
class="update-btn">

Send OTP

</button>

<a
href="/profile"
class="cancel-btn-outlined">

Cancel

</a>

</div>

</form>
            </div>
        </div>
    </main>

    <!-- Footer -->
    <%- include("../partials/user/footer") %>

    <script>
        lucide.createIcons();

        // Entrance animation
        gsap.from(".change-email-container", {
            y: 30,
            opacity: 0,
            duration: 0.8,
            ease: "power2.out"
        });
        
        gsap.from(".change-email-card", {
            scale: 0.98,
            opacity: 0,
            duration: 0.8,
            ease: "power2.out",
            delay: 0.2
        });


/* =========================
   SAFARI BACK BUTTON FIX
========================= */

window.addEventListener("pageshow", function (event) {

if (event.persisted) {

window.location.reload()

}

})


    </script>
</body>
</html>
